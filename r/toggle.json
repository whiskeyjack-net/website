{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "toggle",
  "type": "registry:ui",
  "title": "Toggle",
  "description": "Switch control with a label and optional description, for a single on/off setting.",
  "categories": [
    "forms"
  ],
  "registryDependencies": [
    "https://whiskeyjack.net/r/utils.json"
  ],
  "files": [
    {
      "path": "components/ui/toggle.tsx",
      "type": "registry:ui",
      "target": "components/ui/toggle.tsx",
      "content": "import * as React from \"react\";\nimport { cn } from \"@/lib/utils\";\n\nexport interface ToggleProps {\n  /** Optional label rendered to the left of the switch. */\n  label?: React.ReactNode;\n  /** Optional helper text rendered below the label. */\n  description?: React.ReactNode;\n  checked: boolean;\n  onChange: (checked: boolean) => void;\n  disabled?: boolean;\n  className?: string;\n  /** Accessible label when no visible `label` is provided. */\n  \"aria-label\"?: string;\n}\n\n/**\n * Switch-style boolean toggle. When `label` is provided, the whole row reads\n * as `[label]  [switch]`; without a label, just the switch renders (use the\n * `aria-label` prop in that case for accessibility).\n */\nconst Toggle = React.forwardRef<HTMLButtonElement, ToggleProps>(\n  ({ label, description, checked, onChange, disabled, className, ...rest }, ref) => {\n    const handleToggle = () => {\n      if (!disabled) onChange(!checked);\n    };\n\n    const switchEl = (\n      <button\n        ref={ref}\n        type=\"button\"\n        role=\"switch\"\n        aria-checked={checked}\n        onClick={handleToggle}\n        disabled={disabled}\n        className={cn(\n          \"relative inline-flex h-6 w-11 shrink-0 rounded-full transition-colors\",\n          \"focus-visible:outline-none focus-visible:ring-2\",\n          \"focus-visible:ring-[var(--color-accent-500)] focus-visible:ring-offset-2\",\n          \"disabled:cursor-not-allowed disabled:opacity-50\",\n          checked\n            ? \"bg-[var(--color-accent-500)]\"\n            : \"bg-[var(--color-warm-200)] dark:bg-[var(--color-neutral-700)]\",\n          !label && className,\n        )}\n        {...(!label ? rest : {})}\n      >\n        <span\n          aria-hidden\n          className={cn(\n            \"absolute top-0.5 left-0.5 h-5 w-5 rounded-full bg-white shadow-md transition-transform\",\n            checked ? \"translate-x-5\" : \"translate-x-0\",\n          )}\n        />\n      </button>\n    );\n\n    if (!label) return switchEl;\n\n    return (\n      <label\n        className={cn(\n          \"flex items-center justify-between gap-3 cursor-pointer\",\n          disabled && \"cursor-not-allowed opacity-50\",\n          className,\n        )}\n      >\n        <span className=\"flex flex-col gap-0.5 min-w-0\">\n          <span className=\"text-xs text-[var(--color-text-muted-light)] dark:text-[var(--color-text-muted-dark)]\">\n            {label}\n          </span>\n          {description && (\n            <span className=\"text-[11px] leading-snug text-[var(--color-text-muted-light)]/80 dark:text-[var(--color-text-muted-dark)]/80\">\n              {description}\n            </span>\n          )}\n        </span>\n        {switchEl}\n      </label>\n    );\n  },\n);\nToggle.displayName = \"Toggle\";\n\nexport { Toggle };\n"
    }
  ],
  "docs": "Presentational and controlled: you own the value and the change handler, so it drops straight into a settings list. The switch stays physical under RTL by convention, since it carries an on/off metaphor rather than a directional one.",
  "meta": {
    "group": "forms",
    "related": [
      "toggle-group"
    ],
    "exports": [
      "Toggle"
    ],
    "siteSlug": "toggles"
  }
}
